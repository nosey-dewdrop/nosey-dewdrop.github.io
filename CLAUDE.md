# damla_portfolio
Personal portfolio, LIVE: damlahelloworld.github.io (username changed from nosey-dewdrop). Damla's BRAND page.

CURRENT DESIGN (2026-07-07): WHIMSY TERMINAL (Advent-of-Code vibe + Damla's warmth). Dark indigo/purple bg (#12102a), MONOSPACE, palette pink+purple+pistachio(fıstık yeşili)+amber on soft white text. Twinkling `*`/`+`/`·` starfield. Nav = `[bracket]` links (active pink). Page titles rendered as `$ command`. Home = a terminal session: whoami / cat about.txt / `ls projects/` dotted-leader list, tip line, `~ ʚɞ ~` signoff, ASCII butterfly mascot top-right. Projects/exp/skills = terminal cards; note overlay = `$ cat` window. Decoration rules Damla set: TEXT/terminal chars ONLY — no vector-render glyphs (★✧⟡🦋 emoji), use `*`/`+`/`·`, `<3` for hearts, kaomoji ( ˶ˆ ᵕ ˆ˶ )/(｡•ᴗ•｡), ASCII-art mascots (she sends them; saved in /tmp/ascii_mascots.txt — butterfly=home, cat=about, more per page). ALL project/exp/edu data preserved from the old build; only presentation changed.

KEY RULE: verbal/copy text (whoami line, about.txt, etc.) — never invent; ask Damla or pull from her real about-me.md (she gave permission for her bio/hakkımda). me.jpg photo slot exists (not added yet).

History: went sky-storybook (rejected "childish") → white/minon editorial (rejected "sevimsiz, no gusto") → this terminal (approved "çok tatlı :D devam et"). Lesson from this session: DON'T screenshot after every tiny emoji tweak — that burns tokens; only render to verify real layout changes. Big redesigns need Damla's reference/approval first.
