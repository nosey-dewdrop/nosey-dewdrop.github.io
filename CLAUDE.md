# damla_portfolio
Personal portfolio, LIVE: damlahelloworld.github.io. Damla's BRAND page.

CURRENT DESIGN (2026-07-10, REWORK SHIPPED): single page, monospace, 3 user themes [night]/[white]/[violet] (violet default, localStorage). Layout top→bottom: header (wordmark "damlahelloworld ⋆˙⟡" = linkedin link with mouse-following "linkedin!" tooltip; right: theme row + [github ↗] under it) → hero typed like a terminal WITH colors as it types, butterfly ASCII top right → colored stat line "40 projects - IT Girl - I am an idea machine - {vibecoder}" (count computed from list) → 3 columns education(14px)/skills(13px)/experience(12px, ALL real entries) → dual opposite marquees, all names colored, uniform * glyph → wall: every project one row (nr colored by status = name color, one-liner, tech), human filter bar (status+tech+search), sorted by REAL last-commit date newest first → detail overlay per project (real live-site copy + real repo file trees from 42 repos, big green typed "live! ↗" on top for live ones) → about.txt (full about-me.md, 2 columns, lilac labels, 13px, star doodle bg right, centered "{i build things come from the heart!}" + ~ ʚɞ ~) → footer centered links + pink @damlahelloworld.

RULES (Damla, non-negotiable): NO bold anywhere (400 only). Sharp corners 0-3px. No borders. Terminal chars only (* + · . -), her ASCII mascots only (butterfly hero, big butterfly behind wall, creature behind cols, stars behind about). Colors mean something: status colors (live green/wip yellow/uni blue/arc dim); filters plain until selected. No "not course projects" style dissing. Copy is Damla's — never invent. mun = "4x mun delegate". snailmail NOT live. IEEE 2025-2026. "live" label ONLY for products that actually work; landing-only projects keep their landing link + preview in the overlay but the typed link says "landing page" (pj.ll / XINFO.landing), wall status stays wip. kitschat was REWORKED into "short story long" (live title), copy updated 10 Tem; deployment URL stays kitschat.vercel.app.

LEFT OFF: published 10 Tem gece; phone pass done; count auto (40+); YKS resolved: single row 'meyvetabagi' (Sira Sende retired, 'koc' word banned, cilek = the AI). OPEN: per-project class-hierarchy docs (modules); products.json extraction (data inline); Damla's copy pass on one-liners; msducky-abone stays PRIVATE (KVKK), never on wall. 10 Tem fix pass: BUILT trees regenerated UNTRUNCATED from live GitHub (41 repos; msducky-abone removed from BUILT entirely); msducky corrected (862 questions, live+preview = msducky.vercel.app doctors site); vibecodedflopware wall status = live. STILL OPEN: stitchu + meyvetabagi live landing URLs needed from Damla (nothing deployed found: no Pages, vercel 404).

Detail spec + plan: PROJECT.md. Old terminal design: git history (pre-rework).
